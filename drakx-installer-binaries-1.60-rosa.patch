--- a/Makefile.config	2012-09-11 01:24:02.000000000 +0400
+++ b/Makefile.config	2012-09-20 21:16:11.603353375 +0400
@@ -2,8 +2,8 @@
 DISTRIB_NAME = "$(_DISTRIB_NAME)"
 DISTRIB_VERSION = $(shell sed -n '/^.*version=\([^,]*\),.*/s//\1/p' /etc/product.id)
 DISTRIB_TYPE = $(shell sed -n '/^.*type=\([^,]*\),.*/s//\1/p' /etc/product.id)
-#DISTRIB_DESCR = "$(_DISTRIB_NAME) release $(DISTRIB_VERSION)"
-DISTRIB_DESCR = "$(_DISTRIB_NAME) $(DISTRIB_VERSION) Alpha (Tenacious Underdog)"
+DISTRIB_DESCR = "$(_DISTRIB_NAME) release $(DISTRIB_VERSION)"
+#DISTRIB_DESCR = "$(_DISTRIB_NAME) $(DISTRIB_VERSION) Alpha (Tenacious Underdog)"
 
 # not config, but useful everywhere :)
 ARCH := $(patsubst i%86,i386,$(shell uname -m))
--- a/mdk-stage1/Makefile.common	2012-09-09 06:05:36.000000000 +0400
+++ b/mdk-stage1/Makefile.common	2012-09-20 21:17:26.639678619 +0400
@@ -57,7 +57,7 @@
 endif
 endif
 
-LDFLAGS = $(CFLAGS) -Wl,-O2,-no-undefined -z relro -Wl,--no-warn-common
+LDFLAGS = $(CFLAGS) -O2,-no-undefined -z relro
 
 # (blino) make sure yynewerror and yyerrlab are uselessly used
 LFLAGS += --nounput
